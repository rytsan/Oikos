# Auditoria S3 — RENDER

## 1. Harness
Estado ORIGINAL — `docs/devolucoes/oikos-s3-render.html`

```
# docs/devolucoes/oikos-s3-render.html — 3 blocos <script>
┌─────────┬────┬───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┬────────┬───────────────────────────────────────────────────────────────────────────────────────┐
│ (index) │ #  │ criterio                                                                                                                                              │ status │ detalhe                                                                               │
├─────────┼────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┼────────┼───────────────────────────────────────────────────────────────────────────────────────┤
│ 0       │ 1  │ 'Roundtrip project->unproject com erro 0 em 5 câmeras/zooms'                                                                                          │ 'PASS' │ '480 casos exatos'                                                                    │
│ 1       │ 2  │ 'Picking exato: 4 cantos do losango caem no tile, e logo além dele não'                                                                               │ 'PASS' │ 'dentro=true fora=true'                                                               │
│ 2       │ 3  │ 'paintOrder: (x+y) ASC, empate y ASC — casa em (10,10) oclui (11,9)'                                                                                  │ 'PASS' │ 'pos(11,9)=3 < pos(10,10)=4 · ordem total=true · cópia=true'                          │
│ 3       │ 4  │ 'isZoneable: água (0,1) não; terra (2..7) sim; fora do mapa, não-inteiro e lixo devolvem false'                                                       │ 'PASS' │ 't0=N t1=N t2=S t3=S t4=S t5=S t6=S t7=S · borda=true · entradas sujas=true'          │
│ 4       │ 5  │ 'Culling: nenhum tile visível descartado, lista transborda a viewport (margem de 1 tile + sprite), índices no domínio, câmera distante devolve vazio' │ 'PASS' │ 'visíveis=361/3136 · cobertura=true · tiles de margem=210 · vazio fora do mapa=true'  │
│ 5       │ 6  │ 'Zoom com ponto-fixo: o ponto de mundo sob o cursor é invariante e o clamp segura [ZOOM_MIN, ZOOM_MAX] (fiação da roda no smoke [browser])'           │ 'PASS' │ 'pior erro = 1.42e-14 · tile sob o cursor estável = (7,0)'                            │
│ 6       │ 7  │ 'draw() não lança e não altera campo algum fora de camera e hover'                                                                                    │ 'PASS' │ 'lançou=false · estado fora de camera/hover idêntico=true · sem canvas (no-op limpo)' │
│ 7       │ 8  │ 'Export exatamente { draw, project, unproject, isZoneable, paintOrder, visibleTiles }, sem extensão silenciosa'                                       │ 'PASS' │ '[draw, isZoneable, paintOrder, project, unproject, visibleTiles]'                    │
│ 8       │ 9  │ 'Performance: 1000x (unproject + visibleTiles) em menos de 5 ms'                                                                                      │ 'PASS' │ '2.267 ms'                                                                            │
│ 9       │ 10 │ '[browser] Variação visual por cache chaveado por seed: draw() não consome o stream principal de rng'                                                 │ 'SKIP' │ 'sem canvas sob Node'                                                                 │
│ 10      │ 11 │ '[browser] Snapshot-diff: mesmo estado desenha igual; tile alterado desenha diferente'                                                                │ 'SKIP' │ 'sem canvas sob Node'                                                                 │
│ 11      │ 12 │ '[browser] Smoke visual: 30 frames com a ilha da seed 42, média abaixo de 17 ms/frame'                                                                │ 'SKIP' │ 'sem canvas sob Node'                                                                 │
└─────────┴────┴───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┴────────┴───────────────────────────────────────────────────────────────────────────────────────┘
OIKOS S3 harness: 9/9 PASS (+3 SKIP [browser])
# resultado: 9/9 PASS (+3 SKIP [browser] — aceite visual pelo dono)
exit=0
```

Ambiente com Canvas simulado (teste de consistência dos critérios visuais):
**12/12 PASS (+0 SKIP)**, tempo médio de frame 0.17 ms.

## 2. Achados — auditor Antigravity (sessão agêntica #2), 2026-09-13

| # | Sev | Achado | Evidência | Proposta |
|---|---|---|---|---|
| 1 | média | Poluição em oceano (`terr === 0`) é ignorada no render: `if (terr === 0) continue;` pula o tile antes de chamar `corDaAgua`, deixando o fundo na cor de mar limpo | `oikos-s3-render.html:639` | Emendar na extração: `if (terr === 0 && !(st.tiles.pollution && st.tiles.pollution[i] > 0)) continue;` |
| 2 | baixa | Câmera não re-centraliza em `reset()` se `draw()` já executou uma vez: a flag `centrado` permanece `true` no escopo do módulo | `:536`, `:619` | Aceitar para integração (S12 UI pode gerenciar câmera) ou verificar troca de identidade de `state` com `st.camera.x === 0 && st.camera.y === 0` |
| 3 | baixa | `camZoom` é testado no harness via invariância matemática em `state.camera`, e não por chamada direta ao helper interno | `:515-525`, harness `:865-885` | Aceito — `camZoom` é interno e fiação da roda é evento DOM verificado no smoke visual |
| 4 | info | `visibleTiles` devolve `Int16Array` e `paintOrder` recebe e devolve índices `y*MAP+x`, viabilizando o teto de 5 ms | `:459-463`, `:480-484` | Ratificar na seção 11 da carta (D33) |
| 5 | info | `isZoneable` implementado com terra (2..7) `true` (inclusive montanha) e água (0,1) `false`; lacuna de fronteira (S9) declarada corretamente | `:442-456` | Ratificar na seção 11 da carta (D33) |
| 6 | info | Separação de critérios `[browser]` executada com rigor: 9 testes headless e 3 visuais com SKIP sob Node, totalizando 12 critérios | harness `:727-729`, `:984` | Formato e contagem em conformidade com D31 e o adendo |

**Greps (seção 3):** `Math.random` 0 fora de comentário · DOM restrito a canvas 2D e eventos de mouse/wheel (permitido pela seção 4.3 e adendo da rodada 3).
**Exports:** `{draw, project, unproject, isZoneable, paintOrder, visibleTiles}` — confere exatamente com a carta.
**Acesso ao estado:** Sempre dinâmico via `O.state.x` em cada frame/chamada; nenhuma captura de referência em closure.
**Hooks do kernel:** Registra `OIKOS.use('Render', { draw })` sem `update`, respeitando o papel de leitor puro.

## 3. Achados — auditor {quem}, {data}

_Aguardando segundo parecer. Escrever sem ler a seção 2._

## 4. Divergência entre auditores

_Sem segundo parecer ainda._

## 5. Resolução do dono

_Aguardando resolução do dono._
